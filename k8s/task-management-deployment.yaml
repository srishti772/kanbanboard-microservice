apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-management-app
spec:
  replicas: 1 
  selector:
    matchLabels:
      app: task-management-app
  template:
    metadata:
      labels:
        app: task-management-app
    spec:
      containers:
      - name: task-management-app
        image: srishti77/task-management:2.0.0
        imagePullPolicy: Always

        ports:
        - containerPort: 8000
        env:
        - name: CONFIG_SERVER_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: config_server_url
        - name: SERVICE_NAME
          value: "taskmanagement"     
        - name: ZIPKIN_BASE_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: zipkin_url 
        - name: MONGODB_USERNAME
          valueFrom:
            secretKeyRef:
              name: myapp-secrets
              key: mongo_root_username
        - name: MONGODB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: myapp-secrets
              key: mongo_root_password
        - name: MONGODB_HOST
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: mongodb_host  
        - name: RABBITMQ_URI
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: rabbitmq_uri  
                                             
---

apiVersion: v1
kind: Service
metadata:
  name: task-management-svc
spec:
  selector:
    app: task-management-app
  ports:
  - port: 80
    targetPort: 8000
